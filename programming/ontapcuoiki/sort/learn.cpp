#include <iostream>
using namespace std;

struct Point {
    double x, y, z;
};

int binary_search(int l, int r, Point a[],  Point t, int mode) {
    if(mode == 1) {
        while(l < r) {
        int mid = (l + r)/2;
            if(a[mid].x == t.x) {
                
            } 
        }

    }
}